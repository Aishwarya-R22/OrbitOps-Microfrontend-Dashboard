'use client';
import {Bell,CheckCircle2,CircleDollarSign,ShieldAlert,UserPlus} from 'lucide-react';import {useState} from 'react';import type {ModuleProps} from '@/lib/module-contract';
const initial=[[ShieldAlert,'New sign-in from Chrome','A new session was detected in Bengaluru, India.','2 min ago'],[UserPlus,'Maya accepted your invite','Maya Chen is now a member of Northstar Labs.','18 min ago'],[CircleDollarSign,'Payment method updated','Your primary card ending in 4821 was changed.','Yesterday'],[CheckCircle2,'Weekly report is ready','Your workspace report for Sep 15–21 is available.','Yesterday']] as const;
export default function NotificationsModule(_:ModuleProps){const[items,setItems]=useState([...initial]);return <section className="module-page"><div className="page-head"><div><p className="eyebrow">Inbox</p><h1>Notifications</h1><p className="subhead">Important workspace events, security alerts, and updates.</p></div>{items.length>0&&<button className="primary-btn" onClick={()=>setItems([])}>Mark all as read</button>}</div>{items.length?<div className="notice-list">{items.map(([Icon,title,body,time])=><article className="card notice" key={title}><span className="activity-icon"><Icon size={16}/></span><div><p>{title}</p><span>{body} · {time}</span></div><button aria-label={`Dismiss ${title}`} onClick={()=>setItems(v=>v.filter(x=>x[1]!==title))}>×</button></article>)}</div>:<div className="card empty-state"><span className="activity-icon"><Bell/></span><h2>You’re all caught up</h2><p>New workspace activity will appear here.</p></div>}</section>}
